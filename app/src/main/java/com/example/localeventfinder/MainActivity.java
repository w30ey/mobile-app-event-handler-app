package com.example.localeventfinder;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

/**
 * This class is intentionally left minimal. 
 * The primary application logic has been moved to the 'activities' package.
 */
public class MainActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // This activity is no longer in use as the main entry point.
        // The launcher activity is now LoginActivity.
    }
}
